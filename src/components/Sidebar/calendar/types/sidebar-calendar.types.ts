import type { Card } from "@/components/ui/card";
import type { ComponentProps } from "react";

/**
 * Props for the SidebarCalendar component, extending the Card component props.
 */
export type SidebarCalendarProps = ComponentProps<typeof Card>;
