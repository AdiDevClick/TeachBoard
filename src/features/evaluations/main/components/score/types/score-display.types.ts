/**
 * @fileoverview This file defines the types for the ScoreDisplay component, which is responsible for displaying a score with appropriate styling based on its value.
 */

/**
 * Props for the ScoreDisplay component.
 */
export type ScoreDisplayProps = Readonly<{
  score: number;
}>;
