import type { AppModalNames } from "@/configs/app.config";

/**
 * Component responsible for handling the deletion of an evaluation
 */
export type EvaluationDeleteProps = Readonly<{
  pageId?: AppModalNames;
}>;
