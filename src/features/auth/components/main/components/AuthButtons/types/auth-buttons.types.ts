import type { PropsWithChildren } from "react";

/**
 * Props for the AuthButtons component, which renders the social login buttons and a separator.
 */
export type AuthButtonsProps = PropsWithChildren;
